package com.datalogic.decode.configuration;

import com.datalogic.device.configuration.*;

/**
 * <code>Maxicode</code> is the class that configures the Maxicode Symbology.
 * 
 */
public class Maxicode extends SymbologyLengths {

	/**
	 * This is the constructor of Maxicode. All symbology options are
	 * initialized by editor.
	 * 
	 * @param editor A {@link PropertyGetter} used to load the current barcode family.
	 */
	public Maxicode(PropertyGetter editor) {
	}

}
