package com.datalogic.decode.configuration;

import com.datalogic.device.configuration.*;

/**
 * <code>PostalJapan</code> is the class that configures the Postal Japan.
 */
public class PostalJapan extends SymbologyBase {

	/**
	 * This is the constructor of PostalJapan. All symbology options are
	 * initialized by editor.
	 * 
	 * @param editor A {@link PropertyGetter} used to load the current barcode family.
	 */
	public PostalJapan(PropertyGetter editor) {
	}
}
