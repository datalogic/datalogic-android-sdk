package com.datalogic.decode.configuration;

import com.datalogic.device.configuration.*;

/**
 * <code>PostalKix</code> is the class that configures the Postal Kix.
 */
public class PostalKix extends SymbologyBase {

	/**
	 * This is the constructor of PostalKix. All symbology options are
	 * initialized by editor.
	 * 
	 * @param editor A {@link PropertyGetter} used to load the current barcode family.
	 */
	public PostalKix(PropertyGetter editor) {
	}
}
