package com.datalogic.device;

/**
 * The <code>BootType</code> enumeration defines the type of boot reason.
 */
public enum BootType {
	/** Software reset */
	RESET,
	/** Factory reset */
	FACTORY_RESET,
	/** Enterprise reset */
	ENTERPRISE_RESET;
}
